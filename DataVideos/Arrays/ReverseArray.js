// Crie um vetor e depois inverta os valores adicionados.

// Forma A

/*function reverseArray1 (a) {

        let b = [ ];

        for(let i; i<a; i++) {
            b.splice(0,i,2);
        } return b

        console.log(reverseArray1(a))
} */

let a = [1,4,3,2];
a.reverse();  // função predefinida do JS
console.log(a); // o vlaor de "a" foi substituído por esse novo

